app-paginate(
  '[itemsTotal]'="itemsTotal" '[itemsOnPage]'="itemsOnPage" '[currentPage]'="currentPage"
  '(currentPageChanged)'="loadProducts($event)"
)

ul
  app-product-list-item(*ngFor="let product of products" '[product]'="product")
  li
  li
  li

app-paginate(
  '[itemsTotal]'="itemsTotal" '[itemsOnPage]'="itemsOnPage" '[currentPage]'="currentPage"
  '(currentPageChanged)'="loadProducts($event)"
)